import { ButtonInscriptionPipe } from './button-inscription.pipe';

describe('ButtonInscriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new ButtonInscriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
